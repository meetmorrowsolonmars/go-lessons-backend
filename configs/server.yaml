server:
  address: :7600
debug_server:
  address: :7601
jwt:
  secret_key: secret
  issuer: education pet project
  access_token_duration: 1h
postgres:
  connection_string: host=localhost port=25432 dbname=postgres user=postgres password=postgres sslmode=disable
