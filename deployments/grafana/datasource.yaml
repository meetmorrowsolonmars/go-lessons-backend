apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    url: http://host.docker.internal:9090
    isDefault: true
    access: proxy
    editable: true
  - name: Tempo
    type: tempo
    url: http://host.docker.internal:3200
    isDefault: false
    access: proxy
    editable: false
